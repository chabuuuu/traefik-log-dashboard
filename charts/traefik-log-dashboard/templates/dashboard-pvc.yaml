{{- if .Values.dashboard.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard-data
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "traefik-log-dashboard.dashboard.labels" . | nindent 4 }}
spec:
  accessModes: [ReadWriteOnce]
  {{- if .Values.dashboard.persistence.storageClass }}
  storageClassName: {{ .Values.dashboard.persistence.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.dashboard.persistence.size }}
{{- end }}
